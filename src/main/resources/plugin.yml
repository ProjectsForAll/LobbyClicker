name: '${name}'
version: '${version}'
main: '${main}'

api-version: '1.21'

authors:
  - Drak

folia-supported: true

depend:
  - BukkitOfUtils

softdepend:
  - PlaceholderAPI

commands:
  clicker:
    description: Open the Cookie Clicker GUI
    usage: /<command>
    aliases: [cookieclicker]
    permission: lobbyclicker.use
  leaderboard:
    description: View the Cookie Clicker leaderboard
    usage: /<command>
    aliases: [lb, clickerlb]
    permission: lobbyclicker.use
  clickeradmin:
    description: Admin commands for LobbyClicker
    usage: /<command> <set|add|reset|info|save|openfor> <player> [amount]
    permission: lobbyclicker.admin
  clickertransfer:
    description: Transfer your clicker realm to another player
    usage: /<command> <player>
    permission: lobbyclicker.use
  clickerdata:
    description: Data management for LobbyClicker
    usage: /<command> migrate <to-db|to-sqlite>
    permission: lobbyclicker.admin

permissions:
  lobbyclicker.use:
    description: Allows using the clicker GUI and leaderboard
    default: true
  lobbyclicker.admin:
    description: Allows admin commands for LobbyClicker
    default: op
  lobbyclicker.profiles.max.1:
    description: Allows 1 profile slot
    default: true
  lobbyclicker.profiles.max.3:
    description: Allows 3 profile slots
    default: op
